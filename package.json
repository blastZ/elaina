{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^17.0.23",
    "lerna": "^3.22.1",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish --registry=https://registry.npmjs.org"
  }
}
